{
  "name": "legendary-club-owner-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for Legendary Club Owner (Efsane Baskan) + Playwright test for the lineup-builder interactive element.",
  "scripts": {
    "serve": "serve . -l 3000",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "serve": "^14.2.3"
  }
}
